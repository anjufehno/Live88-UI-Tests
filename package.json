{
  "name": "live88-ui-tests",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end UI tests for the Live88 demo platform using Playwright",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "show-report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "playwright": "^1.52.0"
  }
}
